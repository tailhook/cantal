html render(metrics):
  .container
    h1
      "All metrics"
      if metrics:
        " ({metrics.length})"
    div
      if metrics:
        for m of metrics:
          pre
            code
              =JSON.stringify(m)

